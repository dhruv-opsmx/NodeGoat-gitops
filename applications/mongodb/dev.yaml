apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dev-mongodb
  namespace: argocd
  labels:
    platform: gitops
    environment: dev
    component: application
spec:
  project: platform-project
  source:
    repoURL: https://github.com/opsmx/nodegoat-gitops.git
    targetRevision: HEAD
    path: charts/mongodb
    helm:
      releaseName: mongodb
      valueFiles:
        - ../../environments/dev/mongodb-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
