apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: staging-mongodb
  namespace: argocd
  labels:
    platform: gitops
    environment: staging
    component: application
spec:
  project: platform-project
  source:
    repoURL: https://github.com/dhruv-opsmx/nodegoat-gitops.git
    targetRevision: HEAD
    path: charts/mongodb
    helm:
      releaseName: mongodb
      valueFiles:
        - ../../environments/staging/mongodb-values.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
